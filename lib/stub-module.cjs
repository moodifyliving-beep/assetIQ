/**
 * Stub for optional/test-only deps (e.g. thread-stream's why-is-node-running, tape)
 * so the bundler can resolve them when node_modules test files are pulled in.
 */
module.exports = function () {}
